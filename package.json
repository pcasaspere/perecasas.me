{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port=3000",
    "deploy": "npx nuxi generate && firebase deploy --only hosting --project perecasas-me",
    "generate": "npx nuxi generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/content": "^2.13.1",
    "@nuxt/devtools": "latest",
    "@nuxt/image": "^1.7.0",
    "@nuxtjs/tailwindcss": "^6.12.0",
    "@types/node": "^20.14.10",
    "daisyui": "^4.12.10",
    "nuxt": "^3.12.3",
    "nuxt-simple-sitemap": "^3.0.11"
  },
  "dependencies": {
    "dayjs": "^1.11.11",
    "firebase": "^10.12.3"
  }
}
